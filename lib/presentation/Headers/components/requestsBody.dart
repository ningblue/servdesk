import 'package:flutter/material.dart';

class RequestsBody extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: const Center(
          child: const Text('这是请求的内容'),
        ));
  }
}